- link:
    ~/.zshrc:
      force: true
      path: zshrc
    ~/.nanorc: nanorc
- shell:
    - ["rm -rf ~/.config/nano && mkdir -p .config && make -C nanorc.d && mv nanorc.d/build ~/.config/nano", "Setting up NanoRC"]