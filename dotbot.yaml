- shell:
    - ["git submodule update --init --recursive", "Setting up submodules"]

- link:
    ~/.zshrc:
      force: true
      path: zshrc
    ~/.nanorc:
      force: true
      path: nanorc
    ~/.gnupg/gpg.conf:
      force: true
      path: gpg.conf
- shell:
    - ["make -C nanorc.d", "Setting up nanorc"]