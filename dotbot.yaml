- shell:
    - ["git submodule update --init --recursive", "Setting up submodules"]

- link:
    ~/.zshrc:
      force: true
      path: zshrc
    ~/.nanorc:
      force: true
      path: nanorc
- shell:
    - ["make -C nanorc.d", "Setting up nanord"]